# RAG Backend environment example
# Phase 1.1 does not require external services or secrets.
#
# Future phases may add database, vector store, cache, search engine, or model API settings here.
# Do not commit real passwords, API keys, or tokens.

SERVER_PORT=8080
